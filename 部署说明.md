# ikuai-geoip-sync 部署说明

## 基本信息

- 服务器：minglei@10.0.0.16
- 部署目录：/vol1/1000/MyFiles/Docker/ikuai-geoip-sync/
- 数据目录：/vol1/1000/MyFiles/Docker/ikuai-geoip-sync/data/
- 容器名：ikuai-geoip-sync
- 访问地址：http://10.0.0.16:8765
- 部署时间：2026-06-07 16:45
- 部署 commit：8b6f49a（master）

## 首次使用

1. 浏览器打开 http://10.0.0.16:8765
2. 系统自动引导设置管理密码（至少 6 位）
3. 登录后在配置页面填写爱快路由器地址、账号、密码，选择需要同步的城市
4. 点击"立即同步"验证连通性

## 首次部署步骤（已完成，仅作记录）

1. 本地通过 rsync 同步代码到部署目录（排除 .env、data/）
2. 在飞牛上创建最小 .env（仅含 HOST_PORT=8765）
3. SSH 到服务器执行 `docker compose up -d --build`
4. 验证容器健康状态和接口可访问性

## 升级步骤

在本地项目目录执行：

```bash
# 1. 同步最新代码（保留服务器上的 data/ 不动）
rsync -avz --delete \
  --exclude='.git' --exclude='__pycache__' --exclude='.venv' --exclude='venv' \
  --exclude='.env' --exclude='data/' --exclude='.idea' \
  ./ 'minglei@10.0.0.16:/vol1/1000/MyFiles/Docker/ikuai-geoip-sync/'

# 2. 在服务器上重建并重启
ssh minglei@10.0.0.16 'cd "/vol1/1000/MyFiles/Docker/ikuai-geoip-sync" && docker compose up -d --build'

# 3. 查看日志确认启动成功
ssh minglei@10.0.0.16 'cd "/vol1/1000/MyFiles/Docker/ikuai-geoip-sync" && docker compose logs --tail=50'
```

或直接在 Claude 里输入 `/fnos-check-updates` 自动检测并升级。

## 常用运维命令

```bash
# 查看状态
ssh minglei@10.0.0.16 'cd "/vol1/1000/MyFiles/Docker/ikuai-geoip-sync" && docker compose ps'

# 重启
ssh minglei@10.0.0.16 'cd "/vol1/1000/MyFiles/Docker/ikuai-geoip-sync" && docker compose restart'

# 停止
ssh minglei@10.0.0.16 'cd "/vol1/1000/MyFiles/Docker/ikuai-geoip-sync" && docker compose down'

# 实时日志
ssh minglei@10.0.0.16 'cd "/vol1/1000/MyFiles/Docker/ikuai-geoip-sync" && docker compose logs -f'

# 进入容器
ssh minglei@10.0.0.16 'docker exec -it ikuai-geoip-sync sh'
```

## 数据备份

所有持久化数据都在 `/vol1/1000/MyFiles/Docker/ikuai-geoip-sync/data/` 下，备份此目录即可。

- `data/auth.json`：管理密码哈希 + session 签名密钥
- `data/config.json`：爱快连接配置（含密码，已加密传输）
- `data/snapshot.json`：上次同步的 IP 分组快照
- `data/state.json`：上次同步结果
- `data/sync.log`：累积同步日志

旧版本 data 备份（本次重装前）：`/vol1/1000/MyFiles/Docker/ikuai-geoip-sync.data-backup-20260607-164220`，可在确认无需恢复后手动删除。

## 回滚

如果升级后出问题：
1. 在本地 `git checkout` 到上一个稳定 commit
2. 重新执行升级步骤（rsync + docker compose up -d --build）
