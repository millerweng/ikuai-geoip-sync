# ikuai-geoip-sync 部署说明

## 基本信息
- 服务器：minglei@10.0.0.16
- 部署目录：/vol1/1000/MyFiles/Docker/ikuai-geoip-sync/
- 数据目录：/vol1/1000/MyFiles/Docker/ikuai-geoip-sync/data/
- 容器名：ikuai-geoip-sync
- 访问地址：http://10.0.0.16:8765
- 重启策略：always（开机自启、异常退出自动重启）
- 部署时间：2026-05-31

## 功能
- 从 metowolf/iplist 抓中国城市IP段，按月同步到爱快路由器IP分组
- Web UI 可在线编辑配置（城市/cron/上限/账号密码），热重载无需重启
- 支持 IP 归属查询（输入 IP 看在哪个分组）
- 首次访问强制设置管理密码，后续使用 session cookie 鉴权（密码 bcrypt 哈希存于 data/auth.json）

## 关键文件
- .env：HOST_PORT 等启动期配置（无需填写任何密码）
- data/config.json：用户在 Web UI 修改的运行时配置（覆盖 .env 默认值）
- data/auth.json：管理密码哈希 + session 签名密钥（自动生成，勿手动修改）
- data/snapshot.json：上次同步的分组→CIDR 快照，给 IP 查询用
- data/state.json：上次同步结果
- data/sync.log：累积同步日志

## 升级
```bash
rsync -avz --delete \
  --exclude='.git' --exclude='__pycache__' --exclude='.venv' --exclude='venv' \
  --exclude='.env' --exclude='data/' \
  ./ 'minglei@10.0.0.16:/vol1/1000/MyFiles/Docker/ikuai-geoip-sync/'
ssh minglei@10.0.0.16 'cd "/vol1/1000/MyFiles/Docker/ikuai-geoip-sync" && docker compose up -d --build'
```

## 运维
```bash
# 状态
ssh minglei@10.0.0.16 'docker compose -f /vol1/1000/MyFiles/Docker/ikuai-geoip-sync/docker-compose.yml ps'
# 实时日志
ssh minglei@10.0.0.16 'docker logs -f ikuai-geoip-sync'
```
